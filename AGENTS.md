# 图片与任务规则

- 用户上传、模型生成图片必须存放七牛云，不得写入服务器持久目录或提交 Git。校验和处理使用内存，浏览器直传七牛；展示和下载由七牛直接提供。
- 用户原图、未主动公开的结果与配图中间产物使用私有空间。签名 URL 短期有效，不得写入日志、埋点或持久化为公开地址。
- 未提交生成的记录只是临时草稿，不出现在用户任务列表或后台任务记录；只有成功提交才算任务。
- 生图业务事件复用现有 StatEvent。服务端记录提交和执行结果，避免轮询或重复请求重复计数；不记录文件名、图片内容、提示词或访问凭证。
